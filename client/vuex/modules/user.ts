import * as $ from 'jquery';

// initial state
const state = {
    currentUser: JSON.parse($('#user-data').attr('data-value'))
};

// mutations
const mutations = {

};

export default {
    state,
    mutations
};